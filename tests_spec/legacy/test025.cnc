//type for item, components, some of them have equal names

//Is correct: No

[double* item<int> : char a, int* d, short b, myType_t c, double c, unsigned& e, char* f, float g];
<int tag>;
env -> <tag>;
<tag> :: (step);
(step) -> [item];
[item] -> env;
