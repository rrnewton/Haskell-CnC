//type for item, components, only types

//Is correct: No

[double* item<int> : char, int*, short, myType_t, double, unsigned&, char*, float];
<int tag>;
env -> <tag>;
<tag> :: (step);
(step) -> [item];
[item] -> env;
